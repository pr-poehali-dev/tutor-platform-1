-- Код агента пишет журнал публикаций с ON CONFLICT (kind, ref_key),
-- но такого ограничения в таблице не было: вставка падала с ошибкой,
-- а обработчик её молча гасил. Из-за этого агент отчитывался об
-- отправленных постах, а журнал оставался пустым — и защита от
-- повторной публикации одной и той же статьи не работала.
ALTER TABLE t_p78828167_tutor_platform_1.max_channel_posts
  ADD CONSTRAINT max_channel_posts_kind_ref_key_uniq UNIQUE (kind, ref_key);
